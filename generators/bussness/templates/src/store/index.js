import * as T from './types';
import actions from './actions';
import mutations from './mutations';

const state = {
};

const getters = {
};

export default {
    namespaced: true,
    state,
    mutations,
    actions,
    getters
}
